enum AppRoutes {
  HOME = "/",
  SIGNUP = "/signup",
  SIGNIN = "/signin",
  APPOINTMENTS = "/appointments",
  HISTORY = "/history",
  STATS = "/stats",
  CREATE = "/prófile/create",
  PROFILE = "/profile",
  CUSTOMERS = "/customers",
}

export default AppRoutes;
